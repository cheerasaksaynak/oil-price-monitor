/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/oil-price-monitor',
  assetPrefix: '/oil-price-monitor',
  trailingSlash: true,
  reactStrictMode: true,
};

export default nextConfig;
